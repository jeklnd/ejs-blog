<%- include("partials/header") %>
    <h1>Home</h1>
    <p><%= homeContent %></p>
    <% for (var i = 0; i < posts.length; i++) { %>
        <h1><%= posts[i].titleInput %></h1>
        <p>
            <%= `${posts[i].postInput.substring(0, 100)} ...` %>
            <a href=<%= `posts/${posts[i].kebabTitle}` %>>Read More</a>
        </p>
    <% } %>


<%- include("partials/footer") %> 